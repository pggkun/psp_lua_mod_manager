-- Addresses are descriptor addresses, shown as DESC by HITBOX DIAGNOSTICS.
-- Add as many entries as needed. Values are local offsets in game units.
return {
    -- {
    --     address = 0x09BBE580,
    --     kind = "sphere",
    --     radius = 300.0,
    --     a = { 0.0, 0.0, 0.0 }
    -- },
    -- {
    --     address = 0x09D2E240,
    --     kind = "capsule",
    --     radius = 400.0,
    --     a = { 0.0, 0.0, 0.0 },
    --     b = { 0.0, 0.0, -100.0 }
    -- }
}
